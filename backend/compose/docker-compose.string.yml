services:
  merge:
    build:
      context: ./merge
      dockerfile: Dockerfile
    container_name: pentaas_merge
    # Env vars are passed via -e flags in engine.py
    volumes:
      - ./reports:/app/reports
    network_mode: bridge
